#!/usr/bin/ruby
require 'pry'

Dir["./lib/**/*.rb"].each do |file|
  require file
end

p1 = Player.new ConsoleReader
p2 = Player.new ConsoleReader

game = Game.new p1, p2, Rules, ConsoleWriter, Board.new
game.start
