#!/usr/bin/ruby
require 'pry'

Dir["./lib/**/*.rb"].each do |file|
  require file
end

p1 = Player.new ConsoleReader, :white
p2 = Player.new ConsoleReader, :black

game = Game.new p1, p2, Rules, ConsoleWriter, Board.new
game.start
